//undefined for variable
let name

if (name === undefined) {
    console.log("provide a name");
} else {
    console.log(name);
}

// Undefined for function arguments that aren't provided

let square = function (num) {
    console.log(num);
}

